name: Build Windows EXE

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install rectpack pyinstaller

      - name: Build EXE
        run: pyinstaller --onefile --windowed --name generate_cut generate_cut.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: generate_cut-windows
          path: dist/generate_cut.exe
